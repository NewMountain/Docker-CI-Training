##### Chapter 5 ##### 
### btree is the best
deployment:
  dockerhub:
    branch: master
    commands:
      - $DOCKER_HUB_TRIGGER

test:
  post: 
    - bash <(curl -s https://codecov.io/bash)
  override:   
    - mocha 